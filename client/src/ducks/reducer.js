//initial state
const initialState = {};
//constants

//action creators

//reducer
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
